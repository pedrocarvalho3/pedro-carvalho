// crie uma tipagem para o a variável projects
// que é um array de objetos
// que tem como propriedade o id e o title
// que são do tipo number e string respectivamente
export type Project = {
    id: number;
    title: string;
    image: string;
    link: string;
}